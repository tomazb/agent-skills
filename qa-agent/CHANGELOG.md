# Changelog

## 0.1.0

- Normalize qa-agent into a full skill package with metadata, tooling, references, and tests.
- Add explicit mode selector (`review | test-plan | execute | regression | bug-hunt`) with default `review` and two-phase mixed-intent handling.
- Align SKILL.md structure with sibling skills and add mode-aware output contract.
